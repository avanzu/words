{% extends '@App/Layout/layout.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Create account</button>
    {{ form_end(form )}}

{% endblock %}